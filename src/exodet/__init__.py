"""Exoplanet transit detection: preprocessing, BLS search, vetting
features, classification and transit fitting.

Import the pieces directly (``from exodet.pipeline import analyze``);
this module deliberately re-exports nothing, so importing the package
stays cheap and does not drag in the whole scientific stack.
"""
